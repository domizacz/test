<Window x:Class="WPFApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFApp"
        mc:Ignorable="d" Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="379*"/>
        </Grid.RowDefinitions>
        <StackPanel Height="78" VerticalAlignment="Top" Grid.RowSpan="2">
            <Button x:Name="previousImageButton" Content="Previous" HorizontalAlignment="Center" Click="previousImageButton_Click" />
            <Button x:Name="sunInformationButton" Content="Sun Info" HorizontalAlignment="Center" Click="sunInformationButton_Click"/>
            <Button x:Name="nextImageButton" Content="Next" Click="nextImageButton_Click"  HorizontalAlignment="Center" />
        </StackPanel>
        <Image x:Name="comicImage" Grid.RowSpan="2"/>
    </Grid>
</Window>
